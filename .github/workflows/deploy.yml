name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1npm run build
    - name: Run a build script
      run: |
        npm run build
        echo Build
